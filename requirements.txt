# Convenience requirements file for pip users.
# This repo is primarily managed via `pyproject.toml` / `uv.lock`, but this enables:
#   pip install -r requirements.txt

# Install this package (and its dependencies from pyproject.toml)
-e .

# Project dependencies (mirrors `pyproject.toml`)
ftfy>=6.3.1
huggingface-hub>=1.1.6
iopath>=0.1.10
ipykernel>=7.1.0
ipympl>=0.9.3
ipywidgets>=8.1.8
matplotlib>=3.10.8
mlx>=0.30.0
numpy>=2.3.5
pillow>=12.0.0
regex>=2025.11.3
requests>=2.32.5
scikit-image>=0.25.2
scikit-learn>=1.8.0
torch>=2.9.1
torchvision>=0.24.1

# Web app backend dependencies (FastAPI server)
-r app/backend/requirements.txt

